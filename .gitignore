node_modules/
dist/
pr-review-report.json
*.tsbuildinfo

# local secrets — never commit
.cursor-key
.env.local

# local machine settings — not for public repo
.claude/settings.local.json
.cursor/
.worktrees/

# internal dev-process notes — not user-facing docs
docs/superpowers/
